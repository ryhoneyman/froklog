# froklog Copilot Instructions

## Cargo commands
Always pass `--no-default-features` to every `cargo` invocation (build, check, test, clippy, run, etc.).

```
cargo build --no-default-features
cargo check --no-default-features
cargo test --no-default-features
```
